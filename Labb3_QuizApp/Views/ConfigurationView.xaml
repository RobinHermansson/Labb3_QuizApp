<UserControl x:Class="Labb3_QuizApp.Views.ConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Labb3_QuizApp.Views"
             mc:Ignorable="d" 
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             d:DesignHeight="450" d:DesignWidth="800"
             FontSize="12"
             DataContext="{Binding ConfigurationViewModel}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition  Width="2*"/>
            <ColumnDefinition  Width="2*"/>
            <ColumnDefinition  Width="4*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
            <Label Grid.Row="0" FontWeight="Bold" Content="{Binding ActivePack.Name}"></Label>
        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Grid.Row="0" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="center"  Content="{fa:Icon plus}" Margin="5 0 0 0" Command="{Binding AddNewQuestionCommand}"/>
            <Button Grid.Row="0" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="center"  Content="{fa:Icon minus}" Margin="5 0 0 0" Command="{Binding RemoveSelectedQuestionCommand}" CommandParameter="{Binding ElementName=questionListBox, Path=SelectedItem}"/>
            <Button Grid.Row="0" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="center"  Content="{fa:Icon gear}" Margin="5 0 0 0" Command="{Binding OpenOptionsWindowCommand}"/>
        </StackPanel>
        <ListBox x:Name="questionListBox" Grid.Row="1" Grid.ColumnSpan="2" Margin="5 0 0 5" ItemsSource="{Binding ActivePack.Questions}" SelectedItem="{Binding SelectedQuestion, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="Question:" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Query}"/>
                    </StackPanel>
                </DataTemplate>
               
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Column="2" Grid.RowSpan="2">
            <Label FontWeight="Bold" Content="Question:" Margin="20 15 0 0"/>
            <TextBox Text="{Binding SelectedQuestion.Query, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" AcceptsReturn="True" MinHeight="200" Margin="20 0 20 20"></TextBox>
            <Label FontWeight="Bold" Content="Correct Answer:"  Margin="15 0 20 0"></Label>
            <TextBox Text="{Binding SelectedQuestion.CorrectAnswer, UpdateSourceTrigger=PropertyChanged}" Margin="20 0 20 0"></TextBox>
            <Label FontWeight="Bold" Content="First Incorrect Answer:"  Margin="15 0 20 0"></Label>
            <TextBox Text="{Binding SelectedQuestion.IncorrectAnswers[0], UpdateSourceTrigger=PropertyChanged}" Margin="20 0 20 0"></TextBox>
            <Label FontWeight="Bold" Content="Second Incorrect Answer:"  Margin="15 0 20 0"></Label>
            <TextBox Text="{Binding SelectedQuestion.IncorrectAnswers[1], UpdateSourceTrigger=PropertyChanged}" Margin="20 0 20 0"></TextBox>
            <Label FontWeight="Bold" Content="Third Incorrect Answer:"  Margin="15 0 20 0"></Label>
            <TextBox Text="{Binding SelectedQuestion.IncorrectAnswers[2], UpdateSourceTrigger=PropertyChanged}" Margin="20 0 20 0"></TextBox>
        </StackPanel>
    </Grid>
</UserControl>
